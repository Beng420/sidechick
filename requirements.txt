# Required on macOS/Windows/Linux.
mss
numpy
pyautogui
pynput
pywebview

# Optional Windows input backend. Used when input_backend = legacy.
keyboard; platform_system == "Windows"
mouse; platform_system == "Windows"

# Optional Windows-only capture backend.
# Uncomment this on Windows if you want to experiment with screen_backend = "dxcam".
# dxcam
